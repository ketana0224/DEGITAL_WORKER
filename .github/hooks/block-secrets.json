{
  "$schema": "https://aka.ms/copilot-hooks-schema",
  "description": "Digital Worker が出力するファイルに機密情報(APIキー/シークレット)が含まれていないかチェックする (プレースホルダー)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "editFiles",
        "command": "pwsh -NoProfile -Command \"# TODO: ここに secret スキャンの実装を追加\"",
        "blockOnFailure": false
      }
    ]
  }
}
